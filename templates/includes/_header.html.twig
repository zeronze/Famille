<nav class="navbar navbar-expand-lg navbar-light custom-navbar">
  <div class="container">
    <a class="navbar-brand" href="{{ path('app_home') }}"><strong>Calendrier Famille</strong></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto">
<li class="nav-item">
  <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">
    <strong>Accueil</strong>
  </a>
</li>


        {% if app.user %}
          {% if 'ROLE_ADMIN' in app.user.roles %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_parametre_admin') }}"><i class="bi bi-gear-fill fa-xs"></i>Administration</a>
            </li>
          {% endif %}
<li class="nav-item">
  <a class="btn btn-danger" href="{{ path('app_logout') }}">Déconnexion</a>
</li>

        {% else %}
          <!-- Content for the else block if app.user is not defined -->
        {% endif %}
      </ul>
    </div>
  </div>
</nav>

{% for message in app.flashes('warning') %}
    <div class="container mt-3">
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle-fill"></i>
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    </div>
{% endfor %}

{% for message in app.session.flashbag.get('success') %}
    <div class="container mt-3">
        <div class="alert alert-success alert-dismissible fade show" role="alert">
       <i class="bi bi-check-circle-fill"></i>
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    </div>
{% endfor %}
